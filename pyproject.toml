[project]
name = "cookbook"
version = "0.1.0"
description = "CSV Generator for backtesting engine testing"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.6",
    "polars>=1.40.1",
]
